<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
		package="com.t2xm">
	
	<application
			android:allowBackup="true"
			android:icon="@mipmap/ic_launcher"
			android:label="@string/app_name"
			android:roundIcon="@mipmap/ic_launcher_round"
			android:supportsRtl="true"
			android:theme="@style/Theme.Trip2xiamen">
		<activity
				android:name=".activity.MyReviewsActivity"
				android:exported="true" />
		<activity
				android:name=".activity.MyFavouriteActivity"
				android:exported="true" />
		<activity
				android:name=".activity.SettingsActivity"
				android:exported="true" />
		<activity
				android:name=".activity.ReviewActivity"
				android:exported="true" />
		<activity
				android:name=".activity.DetailsActivity"
				android:exported="true" /> <!-- TODO change default activity to StartActivity -->
		<activity
				android:name=".activity.MainActivity"
				android:exported="true">
			<intent-filter>
				<category android:name="android.intent.category.LAUNCHER" />
				
				<action android:name="android.intent.action.MAIN" />
			</intent-filter>
		</activity>
		<activity
				android:name=".activity.SignUpActivity"
				android:exported="true" />
		<activity
				android:name=".activity.StartActivity"
				android:exported="true" />
	</application>

</manifest>